---
description: Initialize the LLM Wiki in a project — creates directory structure and adds conventions to AGENTS.md
---

Initialize the LLM Wiki in the current project. Safe to run on new or existing projects — only adds missing pieces, never overwrites existing content.

When done, use `/llmw-ingest <url>` to start adding content.
