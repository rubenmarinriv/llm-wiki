---
description: Ingest content from a URL into the LLM Wiki — fetches, summarizes, updates entity pages, and maintains the index
---

Ingest web content into the LLM Wiki. Provide one or more URLs.

Usage: `/llmw-ingest https://example.com/article`

The skill fetches each URL, caches the raw text, discusses key takeaways, then integrates the content into the wiki.
